Namespace v2

    ''' <summary>
    ''' 为了方便进行程序的调试，这个模块的对外依赖关系因该是很小的
    ''' 因为这个模块主要是应用于定义文件的格式以及结构
    ''' 为了减少依赖关系，模型的创建应该是放在compiler模型编译器模块之中
    ''' </summary>
    Module NamespaceDoc
    End Module
End Namespace