namespace UnitTests
{		
	TEST_CLASS(LogSeparateUnitTest)
	{
	private:
			const int numerofdatapoints = 100;
			const int rows = 5;
			const int columns = numerofdatapoints;
			double data[rows][columns];

	public:
		TEST_METHOD(MovingAverageTest)
	};
}