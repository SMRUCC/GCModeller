#' visual plot of the multiple omics union data
#' 
const union_render = function(union_data, 
                              id        = "KEGG", 
                              compound  = "compound", 
                              gene      = "gene", 
                              protein   = "protein", 
                              kegg_maps = NULL) {

    const KEGG_maps = __load_kegg_map(kegg_maps);
    
}